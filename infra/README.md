# Infrastructure - PostgreSQL Database

Эта папка содержит Docker Compose конфигурацию для запуска PostgreSQL базы данных.

> ℹ️ **Все остальные сервисы запускаются локально.** Смотрите инструкции по установке:
> - [Windows](../SETUP_WINDOWS.md)
> - [macOS](../SETUP_MACOS.md)

## 🗄️ Что находится в этой папке

- `docker-compose.yml` — конфигурация для запуска PostgreSQL
- `env.example` — пример переменных окружения (не используется при локальном запуске)
- `README.md` — этот файл

## 🚀 Быстрый старт

### Требования
- Docker Desktop
- 2 GB RAM
- 5 GB свободного места

### Запуск PostgreSQL

```bash
cd infra
docker compose up -d
```

Это запустит только PostgreSQL на порту `5432`.

### Параметры подключения

- **Host:** `localhost`
- **Port:** `5432`
- **Database:** `attendance`
- **User:** `attendance_user`
- **Password:** `secure_password_change_me` (можно изменить в `docker-compose.yml`)

### DATABASE_URL для `.env` файлов

```
DATABASE_URL=postgresql://attendance_user:secure_password_change_me@localhost:5432/attendance
```

### Управление контейнером

```bash
# Остановить базу данных
docker compose down

# Остановить и удалить данные
docker compose down -v

# Просмотр логов
docker logs attendance-postgres

# Подключение к PostgreSQL
docker exec -it attendance-postgres psql -U attendance_user -d attendance
```

## 📝 Дальнейшие шаги

После запуска PostgreSQL, следуйте инструкциям для вашей ОС:

- **[Инструкция для Windows](../SETUP_WINDOWS.md)**
- **[Инструкция для macOS](../SETUP_MACOS.md)**

Эти инструкции описывают:
1. Установку необходимого ПО (Node.js, Python, FFmpeg)
2. Настройку всех сервисов
3. Первый запуск приложения

## 🔧 Решение проблем

### База данных не запускается

```bash
# Проверить статус контейнера
docker ps -a | grep postgres

# Просмотреть логи
docker logs attendance-postgres

# Перезапустить
docker compose restart
```

### Ошибка: "port 5432 already in use"

```bash
# Windows - найти процесс на порту
netstat -ano | findstr :5432
taskkill /PID <PID> /F

# macOS/Linux - найти процесс на порту
lsof -ti:5432 | xargs kill -9
```

### Очистка всех данных

```bash
# Остановить и удалить контейнер с данными
docker compose down -v

# Запустить заново
docker compose up -d
```

---

Для полной инструкции по настройке всего проекта смотрите:
- **[Windows](../SETUP_WINDOWS.md)**
- **[macOS](../SETUP_MACOS.md)**





