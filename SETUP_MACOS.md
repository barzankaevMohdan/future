# 🚀 Инструкция по запуску проекта на macOS

Данная инструкция поможет вам настроить и запустить систему учета посещаемости с распознаванием лиц на macOS.

---

## 📋 Требования

### 1. Homebrew
**Менеджер пакетов для macOS**

**Установка:**
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

**Проверка установки:**
```bash
brew --version
```

---

### 2. Node.js и npm
**Версия:** Node.js 18.x или выше

**Установка через Homebrew:**
```bash
brew install node@18
# или для последней LTS версии
brew install node@20
```

**Проверка установки:**
```bash
node --version
npm --version
```

---

### 3. Python
**Версия:** Python 3.10 или 3.11 (рекомендуется 3.10)

> ⚠️ **Важно:** Python 3.12+ не рекомендуется из-за проблем совместимости с некоторыми библиотеками (dlib, opencv).

**Установка через Homebrew:**
```bash
# Python 3.11 (рекомендуется)
brew install python@3.11

# Python 3.10
brew install python@3.10
```

**Проверка установки:**
```bash
python3 --version
pip3 --version
```

> 💡 На macOS используйте `python3` и `pip3` вместо `python` и `pip`

---

### 4. Docker Desktop
**Для запуска PostgreSQL базы данных**

**Установка:**

#### Вариант 1: Через Homebrew (рекомендуется)
```bash
brew install --cask docker
```

#### Вариант 2: Скачать вручную
- [Docker Desktop для macOS](https://www.docker.com/products/docker-desktop/)

**После установки:**
1. Запустите Docker Desktop из Applications
2. Дождитесь полной загрузки (статус должен быть зеленым)

**Проверка установки:**
```bash
docker --version
docker ps
```

---

### 5. FFmpeg
**Для обработки видеопотоков с камер**

**Установка через Homebrew:**
```bash
brew install ffmpeg
```

**Проверка установки:**
```bash
ffmpeg -version
```

---

### 6. Git
**Для клонирования репозитория**

**Установка через Homebrew:**
```bash
brew install git
```

> 💡 На современных macOS Git уже может быть установлен

**Проверка установки:**
```bash
git --version
```

---

### 7. CMake и Build Tools (для Python пакетов)
**Необходимы для компиляции dlib и других пакетов**

**Установка через Homebrew:**
```bash
brew install cmake
```

**Установка Xcode Command Line Tools:**
```bash
xcode-select --install
```

---

## 🔧 Установка и настройка

### Шаг 1: Клонирование репозитория

```bash
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
cd future
```

---

### Шаг 2: Запуск PostgreSQL через Docker

```bash
cd infra
docker run -d \
  --name attendance-postgres \
  -p 5432:5432 \
  -e POSTGRES_DB=attendance \
  -e POSTGRES_USER=attendance_user \
  -e POSTGRES_PASSWORD=secure_password_change_me \
  -v pgdata-attendance:/var/lib/postgresql/data \
  postgres:15-alpine
```

**Или используйте docker-compose:**
```bash
docker compose up -d
```

**Проверка:**
```bash
docker ps
# Вы должны увидеть контейнер attendance-postgres
```

---

### Шаг 3: Настройка Backend

```bash
cd ../backend
```

#### 3.1 Установка зависимостей
```bash
npm install
```

#### 3.2 Настройка переменных окружения
Создайте файл `.env` на основе `env.example`:
```bash
cp env.example .env
```

#### 3.3 Инициализация базы данных
```bash
npm run prisma:migrate
npm run prisma:seed
```

---

### Шаг 4: Настройка Camera Gateway

```bash
cd ../camera-gateway
```

#### 4.1 Установка зависимостей
```bash
npm install
```

#### 4.2 Настройка переменных окружения
```bash
cp env.example .env
```


> ⚠️ `ENCRYPTION_KEY` должен быть таким же, как в backend!

#### 4.3 Копирование Prisma схемы
```bash
npm run prisma:copy-schema
npm run prisma:generate
```

---

### Шаг 5: Настройка Admin Frontend

```bash
cd ../admin-frontend
```

#### 5.1 Установка зависимостей
```bash
npm install
```

#### 5.2 Настройка переменных окружения
```bash
cp env.example .env
```
---

### Шаг 6: Настройка Client Frontend

```bash
cd ../client-frontend
```

#### 6.1 Установка зависимостей
```bash
npm install
```

#### 6.2 Настройка переменных окружения
```bash
cp env.example .env
```
---

### Шаг 7: Настройка Recognition Service (Python)

```bash
cd ../recognition_service
```

#### 7.1 Создание виртуального окружения
```bash
python3 -m venv .venv
```

#### 7.2 Активация виртуального окружения
```bash
source .venv/bin/activate
```

> 💡 Для деактивации используйте команду `deactivate`

#### 7.3 Установка зависимостей
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

> ⚠️ Установка может занять 5-10 минут. Некоторые пакеты (dlib, opencv) требуют компиляции.

---

## ▶️ Запуск приложения

Откройте **5 отдельных вкладок терминала** для каждого сервиса:

### Терминал 1: Backend
```bash
cd ~/путь/к/проекту/future/backend
npm run dev
```
Сервис доступен на: `http://localhost:3000`

---

### Терминал 2: Camera Gateway
```bash
cd ~/путь/к/проекту/future/camera-gateway
npm run dev
```
Сервис доступен на: `http://localhost:4000`

---

### Терминал 3: Admin Frontend
```bash
cd ~/путь/к/проекту/future/admin-frontend
npm run dev
```
Интерфейс доступен на: `http://localhost:5173` (или другой порт, указанный в терминале)

---

### Терминал 4: Client Frontend
```bash
cd ~/путь/к/проекту/future/client-frontend
npm run dev
```
Интерфейс доступен на: `http://localhost:5174` (или другой порт, указанный в терминале)

---

### Терминал 5: Recognition Service (Python)

```bash
cd ~/путь/к/проекту/future
source recognition_service/.venv/bin/activate
python3 -m recognition_service.main \
  --company-slug "demo-company" \
  --backend-url "http://localhost:3000" \
  --refresh-interval 60
```

**Что происходит:**
- ✅ Автоматически обрабатывает все активные камеры компании
- ✅ При добавлении новой камеры через админку она автоматически начнет обрабатываться в течение 60 секунд
- ✅ Каждая камера обрабатывается в отдельном потоке
- ✅ Общий кэш InsightFace модели и эмбеддингов для всех камер

**Отладочное видео:**
- Камера 1: `http://localhost:5001/video_feed`
- Камера 2: `http://localhost:5002/video_feed`
- Камера N: `http://localhost:500N/video_feed`

---

## 🔐 Первый вход

### Админ-панель

**Суперадмин:**
- Email: `superadmin@system.com`
- Пароль: `SuperAdmin123!`

**Админ компании:**
- Email: `admin@demo.com`
- Пароль: `Admin123!`

### Клиентская панель

Используйте те же учетные данные.

---

## 📹 Добавление камеры

1. Войдите в админ-панель как **Админ компании** (`admin@demo.com`)
2. Перейдите в раздел **Камеры**
3. Нажмите **Добавить камеру**
4. Заполните форму:
   - **Название:** Например, "Вход в офис"
   - **IP адрес:** `192.168.1.10`
   - **Порт RTSP:** `554` (по умолчанию)
   - **Имя пользователя:** `admin`
   - **Пароль:** `your_camera_password`
   - **RTSP путь:** `/ISAPI/Streaming/Channels/101` (для Hikvision)
   - **Статус:** Активна ✅
   - **Распознавание включено:** Да ✅
5. Нажмите **Сохранить**

**Что происходит дальше:**
- ✅ Видеопоток становится доступен: `http://localhost:4000/streams/{camera_id}.mjpg`
- ✅ В течение 1 минуты Recognition Service автоматически начнет обрабатывать камеру (если запущен в multi-camera режиме)
- ✅ События распознавания будут появляться в разделе **События**

> 💡 **Совет:** В разделе **Live** можно проверить, что видео с камеры отображается корректно

---

## 🛠️ Полезные команды

### База данных
```bash
# Остановить PostgreSQL
docker stop attendance-postgres

# Запустить PostgreSQL
docker start attendance-postgres

# Пересоздать базу данных
cd backend
npm run prisma:migrate:reset
npm run prisma:seed
```

### Обновление зависимостей
```bash
# Backend
cd backend
npm install

# Camera Gateway
cd camera-gateway
npm run prisma:copy-schema
npm install

# Recognition Service
cd recognition_service
source .venv/bin/activate
pip install -r requirements.txt
```

### Очистка
```bash
# Очистка node_modules
npm run clean  # в каждом Node.js проекте

# Очистка Python кэша
cd recognition_service
deactivate
rm -rf .venv
python3 -m venv .venv
```

---

## 🐛 Решение проблем

### Ошибка: "Cannot connect to database"
- Убедитесь, что Docker контейнер с PostgreSQL запущен: `docker ps`
- Проверьте `DATABASE_URL` в `.env` файлах

### Ошибка: "ffmpeg not found"
- Убедитесь, что FFmpeg установлен: `ffmpeg -version`
- Переустановите FFmpeg: `brew reinstall ffmpeg`

### Ошибка: "Failed to open camera"
- Убедитесь, что camera-gateway запущен и доступен
- Проверьте URL камеры: откройте в браузере `http://localhost:4000/streams/1.mjpg`
- Проверьте, что RTSP-поток камеры работает (используйте VLC)

### Ошибка при установке Python пакетов
- Убедитесь, что установлены Xcode Command Line Tools:
  ```bash
  xcode-select --install
  ```
- Установите CMake:
  ```bash
  brew install cmake
  ```
- Попробуйте установить пакеты по одному:
  ```bash
  pip install numpy==1.24.3
  pip install opencv-python-headless==4.5.5.64
  pip install dlib
  ```

### Ошибка: "Address already in use"
```bash
# Найти процесс на порту (например, 3000)
lsof -ti:3000

# Завершить процесс
kill -9 $(lsof -ti:3000)
```

### Ошибка: "xcrun: error: invalid active developer path"
```bash
# Установите Xcode Command Line Tools
xcode-select --install
```

### Проблемы с разрешениями
```bash
# Исправить разрешения для node_modules
sudo chown -R $(whoami) node_modules

# Исправить разрешения для Python venv
sudo chown -R $(whoami) .venv
```

---

## 🍎 Особенности macOS

### Apple Silicon (M1/M2/M3)
Некоторые пакеты могут требовать установки через Rosetta:

```bash
# Установка Rosetta (если еще не установлена)
softwareupdate --install-rosetta

# Для проблем с dlib на ARM:
arch -x86_64 pip install dlib
```

### Разрешения для камеры (если используется встроенная камера)
1. Откройте **System Settings** → **Privacy & Security** → **Camera**
2. Разрешите доступ для Terminal/iTerm

---

## 📚 Дополнительные ресурсы

- [Homebrew документация](https://docs.brew.sh/)
- [Node.js документация](https://nodejs.org/docs/)
- [Python документация](https://docs.python.org/3/)
- [Docker документация](https://docs.docker.com/)
- [FFmpeg документация](https://ffmpeg.org/documentation.html)
- [Prisma документация](https://www.prisma.io/docs/)
- [Vue.js документация](https://vuejs.org/guide/)

---

## 💡 Советы

1. **Используйте iTerm2** вместо стандартного Terminal для лучшего опыта
2. **Установите Oh My Zsh** для улучшения командной строки:
   ```bash
   sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
   ```
3. **Используйте VSCode** с расширениями:
   - ESLint
   - Prettier
   - Python
   - Prisma
   - Vue Language Features (Volar)
4. **Добавьте алиасы** в `~/.zshrc`:
   ```bash
   alias py="python3"
   alias pip="pip3"
   alias ll="ls -lah"
   ```

---

## 🚀 Быстрый старт (скрипт)

Создайте файл `start-all.sh` в корне проекта:

```bash
#!/bin/bash

# Цвета для вывода
GREEN='\033[0;32m'
NC='\033[0m'

echo -e "${GREEN}🚀 Запуск всех сервисов...${NC}"

# Запуск Backend
echo -e "${GREEN}▶️  Запуск Backend...${NC}"
cd backend && npm run dev &

# Запуск Camera Gateway
echo -e "${GREEN}▶️  Запуск Camera Gateway...${NC}"
cd ../camera-gateway && npm run dev &

# Запуск Admin Frontend
echo -e "${GREEN}▶️  Запуск Admin Frontend...${NC}"
cd ../admin-frontend && npm run dev &

# Запуск Client Frontend
echo -e "${GREEN}▶️  Запуск Client Frontend...${NC}"
cd ../client-frontend && npm run dev &

echo -e "${GREEN}✅ Все сервисы запущены!${NC}"
echo -e "${GREEN}Нажмите Ctrl+C для остановки${NC}"

wait
```

Сделайте скрипт исполняемым:
```bash
chmod +x start-all.sh
```

Запустите все сервисы:
```bash
./start-all.sh
```

---

Если возникли проблемы, откройте Issue в репозитории проекта! 🚀

